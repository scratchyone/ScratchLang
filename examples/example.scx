sprite Dog {
    function varstuff() {
        const data = "epic return value"
        const tmp = data
        return tmp
    }
    function main() {
        const message = "Will now show return value:"
        say(message, 3)
        Dog.varstuff()
        say(lastRet, 2)
        say("Displayed!")
    }
}