sprite Dog {
    function varstuff() {
        var x = "variable named x"
        return x
    }
    function main() {
        var x = "Will now show return value:"
        say(x, 3)
        Dog.varstuff()
        say(lastRet, 2)
    }
}